---
namespace: dev
images:
  - name: brizaldi/simple-bank
    newTag: sha-def8141de7db720aaa96b90ca8c2753cc8fc069a
resources:
  - ../../../simple-bank-base
patches:
  - target:
      kind: Deployment
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/envFrom/0/name
        value: simple-bank-secret
  - target:
      kind: Deployment
    patch: |-
      - op: add
        path: /spec/template/spec/imagePullSecrets
        value: [{ name: dockerconfigjson }]